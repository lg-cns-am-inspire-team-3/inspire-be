server:
  port: 8080
  
spring:
  application:
    name: inspire-be

  profiles:
    active: dev

  # oauth2 client configuration if needed

management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      show-details: always

jwt:
  access:
    secret: "yeonjin-inspire-project-secret-key-2026-fighting-fighting"
    expires: 1800000  
  refresh:
    secret: "yeonjin-inspire-project-refresh-key-2026-fighting-fighting"
    expires: 1209600000 